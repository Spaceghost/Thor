# Thor

Thor is the newer Odin compiler architecture from graphitemaster, carried here as the Spaceghost working fork.

The canonical branch in this fork is `master`. It contains the upstream `main` history plus the modern parser work and the C99/acceptance bring-up used by the Bor language shootout. The historical `main` branch is retained only until branch migration is complete.
